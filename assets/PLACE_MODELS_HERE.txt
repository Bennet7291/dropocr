这个目录需要放置三个文件才能编译：

  det.onnx       检测模型
  rec.onnx       识别模型
  rec_dict.txt   识别字典

具体获取/转换步骤见仓库根目录的 MODELS.md。

放好这三个文件后，删除本说明文件（或不删也不影响编译，
因为 main.rs 里只用 include_bytes!/include_str! 引用了
上面那三个具体文件名，不会引用这个 txt）。
